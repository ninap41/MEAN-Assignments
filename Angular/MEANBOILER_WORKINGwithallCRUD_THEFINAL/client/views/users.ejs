<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta users="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/skeleton.css'/>   
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Quoting Dojo</title>
    <style>
            #Padding_Please{width:30%;vertical-align:top;display: inline-block; padding: 12rem; padding-top:0px;}
           
        </style>
    
</head>

<body>
<div id="Padding_Please">
<table value="Squish">
<h1>Users</h1>
    <tr>    
            <th>First Name:</th>
            <th>Last Name:</th>
            <th>Email:</th>
            <th>Birthday:</th>
            <th></th>
           
    </tr>
    <% if (  logged_user) { %>
       <h1> Welcome, <%= logged_user.first_name %>!</h1>
       <a href="/logout"><button>Logout</button></a>
        <% } %>
  
            <% if (users) { %>
                <% for (var i in users) { %>
                    <tr>
                        <td><i>"<%= users[i].first_name %>"</i></td> 
                        <td><i>"<%= users[i].last_name %>"</i></td> 
                        <td><i>"<%= users[i].email %>"</i></td> 
                        <td><i>"<%= users[i].birthday %>"</i></td> 
                        <% if (  logged_user) { %>
                        <% if (  users[i]._id ==logged_user._id) { %>
                            <td><a href="/delete/<%= users[i]._id %>"><button>Delete</button></a></td>
                        <% } %>
                        <% } %>
                    
                    </tr> 
                <% } %>
            <% } %>
        
</table> 
<a href="/"><button>Back</button></a>
</div>
    </div>
    
</body>
</html>